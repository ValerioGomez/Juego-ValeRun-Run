rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura/escritura pública (TEMPORAL para testing)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}